# @package _global_

defaults:
  - bool_mask_audio_jepa
  - override /interaction/environment: audio_dataset

interaction:
  environment:
    observation_generator:
      root_dir: ${audio_data_dir}

audio_data_dir: ??? # Specify as command line argument. `python launch.py experiment=<experiment file name> audio_data_dir=<path/to/audio_data>
task_name: bool_mask_audio_jepa_with_dataset
