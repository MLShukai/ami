# @package _global_

defaults:
  - temporal/fundamental
  - override /models: temporal/no_action

interaction:
  agent:
    include_action_modality: False
    initial_action: null

  environment:
    observation_generator:
      _target_: torch.randn

subtask_name: whitenoize_no_action
